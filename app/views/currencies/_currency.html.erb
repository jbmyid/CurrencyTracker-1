<tr id="country_<%= currency.code %>">
  <td class="filter"><%= currency.name %></td>
  <td><%= currency.code %></td>
  <td><%= currency.collected_by?(current_user) ? 'Collected' : 'Not Collected' %></td>
  <td>
  	<%unless currency.collected_by?(current_user)%>
  		<%= check_box_tag "currency_ids[]", currency.code, currency.collected_by?(current_user), class: "currency_check" %>
  	<% end %>
  </td>
  <td><%= link_to 'Show', currency %></td>
</tr>