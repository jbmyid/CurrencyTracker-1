<div id="visited_countries_chart" class="simple_pie_chart" width='300px' height='100px'>
  <table>
    <tr>
      <th>Visited</th>
      <td><%= Country.visited_by(current_user).count %></td>
    </tr><tr>
      <th>Not Visited</th>
      <td><%= Country.not_visited_by(current_user).count %></td>
    </tr>
  </table>
</div>